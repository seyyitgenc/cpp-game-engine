#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

class Texture
{
private:
    /* data */
public:
    Texture(/* args */);
    ~Texture();
};

Texture::Texture(/* args */)
{
}

Texture::~Texture()
{
}
